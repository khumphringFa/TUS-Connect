# leave

## Database configuration

Configure the MySQL connection with environment variables before running the
application:

- `DB_HOST` defaults to `localhost`
- `DB_NAME` defaults to `trinamul_leave`
- `DB_USER` is required
- `DB_PASSWORD` is required

Do not commit real database passwords to this repository.
